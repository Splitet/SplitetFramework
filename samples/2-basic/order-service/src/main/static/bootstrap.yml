spring:
  application:
    name: ${project.artifactId}
  hateoas:
    use-hal-as-default-json-media-type: false
eventapis:
  baseEventsPackage: com.kloia.sample.dto
info:
   build:
      artifact: ${project.artifactId}
      name: ${project.name}
      description: ${project.description}
      version: ${project.version}